import { GrandPrixPipe } from './grand-prix.pipe';

describe('GrandPrixPipe', () => {
  it('create an instance', () => {
    const pipe = new GrandPrixPipe();
    expect(pipe).toBeTruthy();
  });
});
